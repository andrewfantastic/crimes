// SINGULAR form here, even though the entity is the same conceptual one.
export function listWorkspace(): unknown[] {
  return [];
}

export function getWorkspace(id: string): unknown {
  return { id };
}
