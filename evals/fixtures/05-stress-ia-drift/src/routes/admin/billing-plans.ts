// Mirrors src/routes/billing-plans.ts (top-level /billing-plans) at the
// admin destination /admin/billing-plans. Two routes, same conceptual
// surface, no nav or doc link between them — parallel_destination.
export function listAdminBillingPlans(): unknown[] {
  return [];
}
