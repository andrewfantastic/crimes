// Intentional fixture: contributes `account` and `subscription` tokens to
// the IA index. Helps demonstrate Concept Alias Drift on the tenant group.

export default function SubscriptionPage() {
  return (
    <main>
      <h1>Your subscription</h1>
    </main>
  );
}
